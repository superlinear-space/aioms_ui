service_model:
  - control_plane_ping_server_check R6500
exporters_constraints:
  control_plane_blackbox_exporter:
    port: 9115
    modules:
      - icmp
      - tcp_connect
    capabilities:
      - --config.file=/etc/blackbox_exporter/config.yml
